The pictures in the readme. Taken by `pnpm screenshots`, not by hand.

That command throws away an install, sets a fresh one up, fills it with
`seed:demo` and captures every shot in one pass, so none of them can be
older than the rest. Adding or retaking one is
`app/Client/screenshots/capture.spec.ts`.
